# 🎮 OC Pixel Adventure · 交接模版
接手人：下一个 AI / 未来的你 · 会话截止：2026-09-20

## 📍 现状

| 项 | 值 |
|---|---|
| 线上地址 | https://game.mimitu.top |
| 源码仓库 | https://github.com/baimimitu-stack/oc-pixel-adventure |
| 远端 HEAD | \890be5e\ fix(dialogue): light theme + sanitize AI output |
| 本机状态 | **有大量未提交改动**（相对 \890be5e\），**未 push、未部署** |
| 本地源码 | \C:\\直播用\\Claude\\oc-pixel-adventure\（本源） |
| 构建副本 | \C:\\Users\\15648\\Downloads\\oc-pixel-adventure\（构建用） |
| Node | \C:\\Users\\15648\\node-portable\\node-v22.20.0-win-x64\\\（便携版；系统 npm 坏了别用） |
| 便携 git | \D:\\DST\\Git\\cmd\\git.exe\ |
| gh cli | 已登录 \aimimitu-stack\ |
| 用户 | baimimitu@gmail.com · GitHub \aimimitu-stack\ |
| 偏好 | 中文、简短、用「你」、别堆粗体、每段 3–4 句；命令别一次塞太多；出错让贴输出/截图 |

## 🖥️ 服务器

| 项 | 值 |
|---|---|
| 阿里云 ECS | \47.251.97.200\（新加坡，约 148ms） |
| SSH | 阿里云 Workbench 更稳；用户 \dmin\，需 sudo |
| 1Panel | http://47.251.97.200:8090/5b10fa36ef · 用户 \360a402e5d\ |
| Web | **Caddy**（不是 nginx，不要装 nginx） |
| Caddyfile | \/etc/caddy/Caddyfile\（追加 game.mimitu.top 块；mcp.mimitu.top 已存在） |
| 静态目录 | \/var/www/oc-pixel-adventure/\（属主 caddy:caddy） |
| 注意 | 别碰 8000 端口；无 unzip 用 \python3 -m zipfile -e\；Workbench 只能单文件，zip 中转 |

## ✅ 远端已上线（至 890be5e）

- v0.2 亮化 UI / 假版本假模型修复 / 对话退出 / Ctrl+V 上传 / 隐私中心
- v0.3 三家 API（Gemini / DeepSeek / OpenAI 兼容）
- v0.4 纯静态 SPA + game.mimitu.top
- v0.4.1 对话浅色字 + AI 输出 sanitize + 初版 prompt 约束

## ✅ 本机已做完、未提交

1. **音量** — \src/services/sound.ts\：masterGain + \oc_game_volume\；\MainMenuModal\ 设置滑条
2. **快存/快读** — \src/App.tsx\ 顶栏按钮；槽 0；**F5 快存 / F9·F10 快读**
3. **角色卡 MVP** — \src/services/companionCard.ts\ + \CompanionCardPanel.tsx\；OCStudio 导入导出 \.companion.json\；Tavern V3 反填
4. **记忆库 MVP** — \src/services/memory.ts\：diary/permanent；按 npc+oc 隔离；对话写入；主菜单记忆库 tab；JSONL 导入导出；\docs/memory-lite.md\；\	ests/memory.test.cjs\
5. **好感独立** — \src/services/affection.ts\，key \oc_game_npc_affection_v1\，作用域 \world:N\ + ocId；对话改走它；旧 \OC.affection\ 留着不用；存档快照会带；重置/清空会清
6. **提示词** — \src/services/npcPrompt.ts\：好感分档语气；硬限制第一人称 / 不知就不知 / 禁八股与 AI 腔 / 少描写 / 可短答 / 禁全知旁白；随机事件提示同步收紧；\StoryDialogueModal\ 已接入

\	sc --noEmit\ 已通过。用户说过：**先别推，自己先看**。

本机交接副本：\docs/HANDOFF-2026-09-20.md\

### 未提交文件

**改：** \src/App.tsx\ \MainMenuModal.tsx\ \OCStudio.tsx\ \StoryDialogueModal.tsx\ \GameCanvas.tsx\ \db.ts\ \sound.ts\ \	ypes.ts\

**新：** \ffection.ts\ \memory.ts\ \companionCard.ts\ \
pcPrompt.ts\ \CompanionCardPanel.tsx\ \MemoryLibrary.tsx\ \docs/memory-lite.md\ \docs/HANDOFF-2026-09-20.md\ \	ests/*\ \package-lock.json\

## ⏳ 待确认

- [ ] 验收：音量 / 快存快读 / 角色卡 / 记忆库 / 好感分 NPC / 新语气
- [ ] 点头后再：commit → push → build → 部署
- [ ] 线上回归：两个世界 NPC 各聊几句，好感分开、记忆不串、无注入

## 🗝️ 关键 key

| key | 用途 |
|---|---|
| \oc_game_volume\ | 主音量 |
| \oc_game_memory_v1\ | 记忆（含 tombstone） |
| \oc_game_npc_affection_v1\ | NPC×OC 好感，如 \world:1::ocId\ |

记忆归 NPC×OC；升永久改 tier；抹除写 tombstone。Prompt：permanent + 近 6 条 diary + OC 摘要，再进 \
pcPrompt\。好感成就按**单个 NPC** 到 30。

## 🔧 构建部署

\\\at
cd /d C:\\直播用\\Claude\\oc-pixel-adventure
C:\\Users\\15648\\node-portable\\node-v22.20.0-win-x64\\npm.cmd run build
\\\

\dist\\\ → zip → \/tmp\ →

\\\ash
python3 -m zipfile -e dist.zip /tmp/oc-dist
sudo rm -rf /var/www/oc-pixel-adventure/*
sudo cp -r /tmp/oc-dist/* /var/www/oc-pixel-adventure/
sudo chown -R caddy:caddy /var/www/oc-pixel-adventure
\\\

## ⚠️ 坑

- 别用 \D:\\DST\ 里的坏 node；别动 SillyTavern 目录
- Workbench 单文件；\/var/www\ 要 sudo；服务器用 python zipfile
- 全局浅色会打穿局部深色；对话正文用 inline style
- 好感别再挂回 OC；推送前先问用户

## 📚 链接

- 游戏仓：https://github.com/baimimitu-stack/oc-pixel-adventure
- 记忆规范：https://github.com/baimimitu-stack/memory-core（\规范/companion-v1.2.md\）
- 提示词思路参考：https://github.com/zonde306/ST-Prompt-Template（未引入引擎）

---

**下次开始说：** \打开交接模版接着做 [验收 / 提交推送部署 / 其他]\
